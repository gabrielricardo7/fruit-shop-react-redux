import styled from "styled-components";

export const List = styled.section`
    display: flex;
    flex-flow: row;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
    padding: 0;
    padding-top: 75px;
`;